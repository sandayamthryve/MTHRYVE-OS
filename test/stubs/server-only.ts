// Test stub for the `server-only` package, which throws when imported outside a
// Next.js React Server Component bundle. The import-engine unit tests run the
// server-only spreadsheet parser directly under Node, where this no-op is safe.
export {};
