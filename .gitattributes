# Normalize line endings: store LF in the repo, let Git handle checkout.
# Keeps diffs clean across Windows/macOS/Linux and Vercel's Linux builders.
* text=auto eol=lf

# Binary assets — never touch line endings.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
